/** Movimentos de pátio (lift/shift) por operador de empilhadeira por turno. */
export const MOVIMENTOS_POR_OPERADOR_EMPILHADEIRA = 15;

/** Check-ins de gate por checker por turno. */
export const CHECKINS_POR_GATE_CHECKER = 20;

/** Horizonte padrão de projeção de gargalos (dias). */
export const WORKFORCE_PLANNING_HORIZON_DIAS = 7;
